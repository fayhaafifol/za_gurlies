import 'package:flutter/material.dart';


class owl extends StatelessWidget {
  const owl({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Owls", style: TextStyle(fontSize: 26)),

      ),
    );
  }
}