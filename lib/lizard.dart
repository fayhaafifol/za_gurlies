import 'package:flutter/material.dart';


class lizard extends StatelessWidget {
  const lizard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Lizards", style: TextStyle(fontSize: 26)),

      ),
    );
  }
}